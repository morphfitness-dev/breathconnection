# Backend
DATABASE_URL="file:./prisma/dev.db"
JWT_SECRET="change-this-to-a-long-random-string-in-production"
PORT=3000

# Mobile — set BASE_URL in mobile/src/api/client.ts for production
# The app uses http://localhost:3000/api in dev mode (__DEV__ = true)
